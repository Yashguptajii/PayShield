{
  "name": "payshield",
  "version": "1.0.0",
  "private": true,
  "description": "PayShield Payment Security System",
  "scripts": {
    "install:all": "npm install && npm --prefix authService install && npm --prefix payService install && npm --prefix riskService install && npm --prefix frontend install",

    "auth": "npm --prefix authService run dev",
    "payment": "npm --prefix payService run dev",
    "risk": "npm --prefix riskService run dev",
    "frontend": "npm --prefix frontend run dev",

    "dev": "concurrently -k -n AUTH,PAYMENT,RISK,FRONTEND \"npm run auth\" \"npm run payment\" \"npm run risk\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}